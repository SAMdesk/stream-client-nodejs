{
  "name": "@samdesk/stream-client",
  "version": "1.6.0",
  "description": "Socilgist stream client for NodeJS",
  "main": "index.js",
  "scripts": {
    "prepublish": "tsc",
    "postversion": "git push && git push --tags"
  },
  "files": [
    "dist/**/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SAMdesk/stream-client-nodejs.git"
  },
  "author": "Socialgist, forked",
  "devDependencies": {
    "@types/node": "^10.9.4"
  }
}
